# TODO

- [ ] Benchmark the 90,000-token total-context automatic compaction threshold with task-aware compaction and checkpoints against host/model defaults on comparable substantial tasks. Keep model, reasoning effort, and acceptance criteria consistent. Compare preserved decisions and constraints, repeated investigation, verification results, compaction interruptions, completion time, and token usage or cost. Use the results to decide whether to retain or raise the threshold; do not assume 90k prevents hallucinations. Deferred until explicitly requested.
